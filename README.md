# School Campus Hub

## Root Commands

From the repository root you can now use:

```bash
npm run start
npm run build
```

- `npm run start` starts the API server and frontend development workspace.
- `npm run build` runs the workspace typecheck, then builds all packages that expose a build script.

This repo still uses pnpm workspaces for dependency installation and workspace resolution.
